# Cli to implement

--workflow-omit

--only-workflow

--config-file

--github-action-omit

-----

## Implemented

--minimum-nodejs-version (default: 24)

=> Recursively checks that every action (including composite and local composite dependencies) runs on at least this Node.js major version. Logs an error and exits with code 3 otherwise. Set to 0 to disable.
